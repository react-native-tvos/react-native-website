{/* @generated by scripts/src/generate-tv-api.ts — TV props.
Source: react-native-tvos Libraries/Components/TV/TVViewPropTypes.d.ts (TVViewProps).
Do not edit by hand; run `yarn generate:tv-api`. */}

### `autoFocus`

| Type      |
| --------- |
| `boolean` |

---

### `destinations`

Additional properties needed for flow checks on TVFocusGuideView

| Type       |
| ---------- |
| `Object[]` |

---

### `enabled`

| Type      |
| --------- |
| `boolean` |

---

### `focusable`

Whether this `View` should be focusable with a non-touch input device, eg. receive focus with a hardware keyboard.

| Type      |
| --------- |
| `boolean` |

---

### `hasTVPreferredFocus`

May be set to true to force the focus engine to move focus to this view.

| Type      |
| --------- |
| `boolean` |

---

### `isTVSelectable` <div className="label ios">iOS</div>

**Deprecated.** Replaced by `focusable`.

| Type      |
| --------- |
| `boolean` |

---

### `nextFocusDown`

TV next focus down (see documentation for the View component).

| Type     |
| -------- |
| `number` |

---

### `nextFocusForward` <div className="label android">Android</div>

TV next focus forward (see documentation for the View component).

| Type     |
| -------- |
| `number` |

---

### `nextFocusLeft`

TV next focus left (see documentation for the View component).

| Type     |
| -------- |
| `number` |

---

### `nextFocusRight`

TV next focus right (see documentation for the View component).

| Type     |
| -------- |
| `number` |

---

### `nextFocusUp`

TV next focus up (see documentation for the View component).

| Type     |
| -------- |
| `number` |

---

### `onPressIn`

| Type                   |
| ---------------------- |
| `(event: any) => void` |

---

### `onPressOut`

| Type                   |
| ---------------------- |
| `(event: any) => void` |

---

### `safePadding`

| Type             |
| ---------------- |
| `string \| null` |

---

### `scrollSnapAlign`

Scroll snap alignment (used with snapToAlignment="item" in ScrollView).

| Type                             |
| -------------------------------- |
| `("start" \| "center" \| "end")` |

---

### `scrollSnapOffset`

Per-item scroll snap offset in dp/pt. When set on a View inside a
ScrollView with `snapToAlignment="item"`, the focus engine lands the
View's top at viewport y = scrollSnapOffset. Standalone alternative to
`scrollSnapAlign` — set one or the other; if both are set on the same
View, `scrollSnapOffset` takes precedence.

| Type     |
| -------- |
| `number` |

---

### `tvParallaxProperties` <div className="label ios">iOS</div>

_(Apple TV only)_ Object with properties to control Apple TV parallax effects.

| Type                       |
| -------------------------- |
| `TVParallaxPropertiesType` |

---
